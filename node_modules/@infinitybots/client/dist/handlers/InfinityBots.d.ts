/// <reference types="node" />
import { EventEmitter } from 'events';
import { BotStatistics } from '../typings';
interface InfinityOptions {
    auth?: string;
    disableRatelimitHandler?: boolean;
    onlyEmit?: boolean;
}
export declare class InfinityBots extends EventEmitter {
    private options;
    constructor(auth: string, options?: InfinityOptions);
    /**
     * Update options passed to client
     */
    setOptions(options: InfinityOptions): void;
    /**
     * The Base Request (PRIVATE)
     */
    private _request;
    postBotStats(stats: BotStatistics): Promise<BotStatistics>;
}
export {};
